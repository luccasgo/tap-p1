spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/tap_atividade
    username: luccasgo
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect